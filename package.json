{
  "name": "postman-newman-jenkins",
  "version": "1.0.0",
  "description": "Project for storing Postman tests",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "api-tests-production": "newman run tests/MelittaCollection.postman_collection.json --reporters cli,junit --reporter-junit-export newman/report.xml" 
  },
  "author": "Rufina Davletova",
  "dependencies": {
    "newman": "^5.3.0"
  }
}
